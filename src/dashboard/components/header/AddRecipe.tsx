import { Button } from "@material-ui/core";
import React from "react";

const AddRecipe = () => {
   return (
      <span className="recipeHeaderRight">
         <Button variant="contained" color="primary">
            Add Recipe
         </Button>
      </span>
   );
};

export default AddRecipe;
