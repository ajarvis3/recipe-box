import { FunctionComponent } from "react";
import MenuBook from "@material-ui/icons/MenuBook";

const RecipeNotesIcon: FunctionComponent = () => {
   return <MenuBook />;
};

export default RecipeNotesIcon;
